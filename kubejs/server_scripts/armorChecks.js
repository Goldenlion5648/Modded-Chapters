

onEvent("player.inventory.changed", event => {
    // var command = `execute as @a[nbt={Inventory:[{Slot:103b,id:"xreliquary:witch_hat"}]}] run team join friends @s`
    // // event.server.runCommandSilent(`tellraw @a "changed"`);
    // event.server.runCommandSilent(command);
    // console.log(event.player.getInventory())
})
